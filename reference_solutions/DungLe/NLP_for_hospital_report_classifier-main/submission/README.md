# Submission

- `output.zip`: V21 production, đã chấm **39.3128**.
- `candidate_v23_symptom-boundary-trim.zip`: V23 boundary ablation, chưa chấm.

Mỗi ZIP chứa đúng `output/1.json` … `output/100.json`.

Không ghi đè `output.zip` bằng candidate cho tới khi leaderboard xác nhận điểm
cao hơn.
