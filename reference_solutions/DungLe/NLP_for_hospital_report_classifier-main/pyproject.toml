[project]
name = "clinical-mentions"
version = "1.13.0"
description = "BamiBERT-gated Vietnamese clinical mention extraction"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/clinical_mentions"]
